# 🚀 Инструкция по развертыванию (Deployment)

Поскольку это Full-Stack приложение (Node.js сервер + React клиент), его нельзя просто залить на GitHub Pages или Vercel. Ему нужен постоянно работающий сервер.

Самый простой и удобный способ — использовать **Railway** или **Render**.

## Способ 1: Railway (Рекомендуемый)

Railway автоматически определит структуру проекта и запустит его.

1.  Зарегистрируйтесь на [railway.app](https://railway.app/) (можно через GitHub).
2.  Нажмите **New Project** -> **Deploy from GitHub repo**.
3.  Выберите ваш репозиторий `TaskManager`.
4.  Railway начнет сборку.

### Настройка переменных окружения
В настройках проекта (Variables) добавьте:
*   `TELEGRAM_BOT_TOKEN`: Ваш токен от BotFather.
*   `PORT`: Railway устанавливает его автоматически, но убедитесь, что сервер слушает `process.env.PORT` (это уже сделано в коде).

### Важное примечание по данным
Так как мы используем локальные JSON файлы (`server/data/`), при каждом новом деплое (обновлении кода) данные могут сбрасываться, если не подключить Persistent Volume (диск).
В Railway можно добавить "Volume" и примонтировать его к папке `/app/server/data`.

---

## Способ 2: VPS (Ubuntu Server)
Если у вас есть свой сервер (например, DigitalOcean, Hetzner, Beget).

1.  **Зайдите на сервер:** `ssh root@ваш_ip`
2.  **Установите Node.js:**
    ```bash
    curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
    sudo apt-get install -y nodejs
    ```
3.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/azhmagin/TaskManager.git
    cd TaskManager
    ```
4.  **Установите зависимости и соберите проект:**
    ```bash
    npm install
    npm run build
    ```
5.  **Запустите сервер (через PM2 для надежности):**
    ```bash
    npm install -g pm2
    pm2 start server/server.js --name "task-tracker"
    ```
    *Сервер будет раздавать и API, и Frontend (из папки `dist`).*

6.  **Настройте Nginx (опционально)**, чтобы открыть доступ по домену на 80 порт.

---

## ⚠️ Важно: Telegram Bot
Для работы бота на сервере, убедитесь, что вы остановили локальную версию (`Ctrl+C` в терминале), иначе будет конфликт обновлений (bot conflict error).
