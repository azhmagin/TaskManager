# Task Tracker - Система Поручений

Современная система управления задачами с возможностью делегирования, Kanban-доской и интеграцией с Telegram.

## 🚀 Как запустить проект

Для работы приложения необходимо запустить две части: сервер (Backend) и клиент (Frontend).

### 1. Подготовка
Убедитесь, что вы находитесь в папке проекта:
```bash
cd /Users/user/.gemini/antigravity/scratch/task-tracker
```

### 2. Зачем нужен сервер?
Сервер отвечает за хранение данных (задачи и пользователи) и работу Telegram бота.
Запустите его командой:
```bash
node server/server.js
```
*Сервер запустится на порту 3001.*

### 3. Запуск интерфейса
Откройте **новое окно терминала** (или вкладку) в той же папке и запустите React-приложение:
```bash
npm run dev
```
*Приложение будет доступно по адресу: http://localhost:5173*

---

## 💾 Как сохранить прогресс

Все данные (задачи и пользователи) автоматически сохраняются в JSON-файлах в папке `server/data/`. Вам не нужно ничего сохранять вручную перед выходом.

### Если вы хотите сохранить код (версионность):
Инициализируйте Git репозиторий, если это еще не сделано:

```bash
git init
git add .
git commit -m "Initial commit: Task Tracker V1"
```

В будущем, чтобы сохранить изменения в коде:
```bash
git add .
git commit -m "Описание изменений"
```

---

## 🛠 Основные возможности

- **Список задач**: Просмотр, добавление, удаление.
- **Kanban доска**: Перетаскивание задач между статусами.
- **Дерево делегирования**: Визуализация поручений.
- **Админ-панель**: Управление пользователями (Через иконку ⚙️).
- **Telegram Бот**: Уведомления о новых задачах и смене статусов.

## 📂 Структура данных
- `server/data/todos.json` - База данных задач.
- `server/data/users.json` - База данных пользователей.
